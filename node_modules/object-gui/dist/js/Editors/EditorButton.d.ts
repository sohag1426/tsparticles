import { EditorItem } from "./EditorItem";
export declare class EditorButton extends EditorItem {
    private readonly id;
    private readonly name;
    private readonly label;
    private readonly autoMap;
    private clickHandler?;
    constructor(data: unknown, id: string, name: string, label: string, autoMap?: boolean);
    click(clickHandler?: () => void): EditorButton;
}
