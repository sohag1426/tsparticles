import { EditorInputBase } from "./EditorInputBase";
export declare class EditorNumberInput extends EditorInputBase {
    private _max?;
    private _min?;
    private _step?;
    private slider?;
    constructor(data: unknown, id: string, name: string, value?: number, autoMap?: boolean);
    private static getDragger;
    step(step: number): EditorInputBase;
    min(min: number): EditorInputBase;
    max(max: number): EditorInputBase;
    addItem(): EditorInputBase;
    addItemGroup(): EditorInputBase;
    addItems(): EditorInputBase;
    updateCollapse(collapsed: boolean): void;
    protected changeEventHandler(): void;
    private getSlider;
    private updateSliderData;
    private updateDragger;
    private updateDraggerEvent;
    private onDrag;
    private drawSlider;
}
